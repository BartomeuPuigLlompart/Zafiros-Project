using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class roomsManager : MonoBehaviour
{
    static public bool cleanRoom;
    // Start is called before the first frame update
    void Start()
    {
        cleanRoom = true;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
